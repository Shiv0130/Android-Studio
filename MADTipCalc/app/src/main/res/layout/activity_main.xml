<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <!-- Input for Bill Amount -->
    <EditText
        android:id="@+id/editBillAmount"
        android:hint="@string/bill_amount_hint"
        android:inputType="numberDecimal"

        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="32dp"
        android:layout_marginTop="80dp"
        android:layout_marginEnd="32dp"

        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Horizontal Row Container for the Buttons -->
    <LinearLayout
        android:id="@+id/layoutButtons"
        android:orientation="horizontal"

        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"

        app:layout_constraintEnd_toEndOf="@+id/editBillAmount"
        app:layout_constraintStart_toStartOf="@+id/editBillAmount"
        app:layout_constraintTop_toBottomOf="@+id/editBillAmount">

        <Button
            android:id="@+id/button15"
            android:text="@string/tip_15"

            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"

            android:layout_marginEnd="4dp"
            />

        <Button
            android:id="@+id/button18"
            android:text="@string/tip_18"

            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"

            android:layout_marginHorizontal="4dp"
            />

        <Button
            android:id="@+id/button20"
            android:text="@string/tip_20"

            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"

            android:layout_marginStart="4dp"
            />
    </LinearLayout>

    <!-- Results Display -->
    <TextView
        android:id="@+id/textResult"
        android:textSize="18sp"
        android:textStyle="bold"
        android:text="Tip: $0.00, Total Bill: $0.00"

        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="40dp"

        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/layoutButtons"
        />
</androidx.constraintlayout.widget.ConstraintLayout>